week 6 — 3 августа – 7 августа 2026

одним предложением: закрыл KAN-56/38/55/6/47/60 , слил опиньон в дев и полез разбираться во external quizzes (вебхуки + урл параметры) , поймал себя на false positive по безопасности , а в пятницу чуть не положил бота вручную восстанавливая вебхук без secret_token — но все обошлось))

над чем работал

KAN-56 (admin v2 не запускает опрос) — два независимых бага: в V1QuizService::quizList() не было orWhereDoesntHave('roles') фолбэка как у соседнего условия с segments , из-за чего кампании без ролей вообще не показывали опрос никому; и target_users_count с nullable|min:0 дефолтился в 0 , из-за чего условие 0 > users_completed всегда false — правки в 5 файлах

KAN-38 и KAN-55
welcome-текст бота откатывался к короткому — BotMessageSeeder использовал updateOrCreate и перетирал правки из admin-v2 при каждом сидинге - я поменял на firstOrCreate
исключающий ползунок рендерился как обычный слайдер — сделал по паттерну KAN-50 , exclusive: bool в SliderConfigResponseDTO + фронт

аудит бэклога / KAN-6 , KAN-47
прошелся по всем 14 открытым пр , свел статусы в джире
KAN-6 — добавил ключ перевода addPhone во все локали
KAN-47 бэк — заменил media_file/media_file_uz на media_by_locale через HasLocalizedMedia + LanguageService::contentLocales() , честно предупредил что локально не протестировал

external quizzes
тимлид поставил цели на август — запустить опиньон и задача разгрести все открытые баги
разобрался в логике external_quizzes от а до я — исходящая часть (куда и с какими url-параметрами уводим юзера на внешний опрос) и входящая (вебхук finishQuiz, как lastQ/SNetDuration матчатся на full_pay/part_pay/no_pay) — записал в journal , technologies-learning/external-quizzes/notes.md
по пути зацепился за то что роут /finish-attempt нигде не проверяет Auth:: — оплата берется из attempt->user_id , начал раскручивать до кросс-аккаунт фрода , тимлид объяснил — токен передается вне браузера юзера до старта кампании, редирект идет напрямую с внешнего ресурса в кор и доступа к токену у юзера физически нет

KAN-60 + аварийный вечер пятницы
попросил у тимлида токены на @opinion_app_dev_bot и @test_insight_bot — получил
тестировал баг с кнопками через telegrambot:poll — команда снимает вебхук на время теста , после руками восстановил через setWebhook но без secret_token , бот словил 401 на все апдейты и перестал отвечать вообще
тимлид скинул креды на препрод , секрет лежит в env в core — нашел , восстановил вебхук с секретом
закрыл KAN-60 , пр #13

техническое решение недели
на видеогейте не стал упираться в свою реализацию раз уже готовое решение тимлида было чище —  через синхронную проверку duration_delay из бэка

что выучил
отсутствие защиты в коде не равно доказанной уязвимости — с /finish-attempt цепочка "нет auth → можно перехватить → cross-account фрод" на каждом звене была непроверенной гипотезой , и я наращивал серьезность вывода не тестируя предыдущее звено
перед тем как трогать вебхук бота вручную — сохранить/найти secret_token заранее , а не восстанавливать конфиг по памяти

итоги недели 🏆
KAN-56 , KAN-38 , KAN-55 , KAN-6 , KAN-47 , KAN-60 — закрыты
видео-гейт — разобрался в решении тимлида , ветки почищены
полное понимание external_quizzes (урл-параметры + вебхуки) задокументировано в journal
security-false-positive по /finish-attempt закрыт
получены токены на dev/test ботов , восстановлен вебхук после моей криворукости
на конец недели только одна таска не решена (KAN-64) , остальное смерджено или пиары готовы

энергия: 9/10
оценка недели: 9/10
